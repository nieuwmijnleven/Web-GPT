# shorts monitor 화면 디자인 통합 구현 계획

## 목적

shorts monitor는 Android 앱 내부 `WebView`에서 유튜브 모바일 웹 쇼츠를 실행하고, 현재 관찰 가능한 쇼츠 목록과 실제 노출 순서를 기록하는 애플리케이션이다.

기존에 인접해 있던 쇼츠 사이에서 새로운 항목이 확인되면 이를 `중간 삽입 의심 이벤트`로 생성하고 사용자에게 알린다.

최초 구현 범위는 다음 기능으로 제한한다.

* 유튜브 쇼츠 목록 기록
* 실제 쇼츠 노출 순서 기록
* 중간 삽입 의심 이벤트 탐지
* 앱 내부 알림과 시스템 알림
* 브라우저 테스트 프로필 생성과 변경
* 쿠키와 사이트 데이터 초기화
* 세션 기록 조회
* 의심 이벤트 사용자 판정
* JSON 및 CSV 내보내기

---

## 첨부 화면 디자인 분석

첨부 디자인에는 다음 핵심 화면이 포함되어 있다.

* 온보딩
* 관찰 홈
* 쇼츠 관찰 중 화면
* 세션 기록
* 중간 삽입 의심 상세
* 브라우저 테스트 프로필

온보딩에는 앱 내부 유튜브 실행, 목록·노출 순서 기록, 중간 항목 감지, 테스트 프로필 변경, 쿠키 초기화, 로컬 기록 보존 기능이 안내되어 있다.

관찰 중 화면에는 현재 프로필과 관찰 개수, 중간 삽입 알림, 현재 영상 정보, 활성 세션 패널이 함께 배치되어 있다.
세션 기록, 의심 이벤트 상세, 프로필 관리 화면도 디자인에 포함되어 있으며, 의심 이벤트에서는 이전 목록과 현재 목록을 비교하고 사용자가 정상 변화·오탐·판단 보류 등을 선택할 수 있도록 구성되어 있다.
프로필 화면의 안내처럼 이 기능은 WebView에서 노출되는 일부 값을 변경하는 것이며 실제 IP 주소나 물리적 하드웨어를 변경하는 기능으로 표현해서는 안 된다.

---

## 디자인 적용 전 수정 사항

### 프로젝트명 통일

첨부 디자인에 표시된 `Shorts Monitor`를 모두 `shorts monitor`로 변경한다.

다음 항목에도 동일하게 적용한다.

* 앱 바 제목
* 온보딩 제목
* 알림 채널 이름
* 내보내기 파일명
* 접근성 설명
* 앱 아이콘 콘텐츠 설명
* 개인정보 안내
* 오류 진단 화면

### 언어 통일

첨부 화면은 영어 중심으로 구성되어 있으므로 구현에서는 Android 문자열 리소스로 분리한다.

기본 표시 언어는 한국어로 하고 다음 항목만 필요에 따라 영문을 병기한다.

* 세션 식별자
* 프로필 식별자
* 영상 식별값
* User-Agent
* 내부 진단 상태
* 내보내기 필드명

### 용어 정리

다음 표현을 사용한다.

* `Insertion Detected` → `중간 삽입 의심`
* `Suspected` → `의심`
* `Normal Change` → `정상 변화`
* `False Positive` → `오탐`
* `Pending` → `판단 보류`
* `Reset Session` → `세션 및 사이트 데이터 초기화`
* `Browser Profiles` → `브라우저 테스트 프로필`

`삽입 확정`, `조작 감지`, `추천 조작 증명`처럼 원인을 단정하는 표현은 사용하지 않는다.

### 색상 체계

첨부 디자인의 밝은 배경과 상태 색상 체계를 유지한다.

* 관찰 활성: 청록색
* 정상 완료: 녹색
* 중간 삽입 의심: 주황색 또는 빨간색
* 오류: 빨간색
* 사용자 판정 완료: 보라색
* 판단 보류: 회색

모든 상태는 색상뿐 아니라 아이콘과 텍스트를 함께 표시한다.

### 내비게이션 보정

첨부 디자인의 하단 내비게이션 구조를 유지한다.

* 관찰
* 기록
* 이벤트
* 프로필
* 설정

좁은 화면에서 텍스트가 겹치지 않도록 짧은 레이블을 사용하고, 글꼴 확대 시에는 아이콘과 선택된 메뉴명만 표시하는 적응형 방식을 적용한다.

쇼츠 관찰 중에는 하단 내비게이션을 숨겨 WebView 영역을 최대화한다.

---

## 최종 화면 구조

```text
온보딩
    ↓
관찰 홈
    ├── 쇼츠 관찰
    ├── 최근 세션
    ├── 의심 이벤트
    ├── 테스트 프로필
    ├── 내보내기
    └── 세션 초기화

쇼츠 관찰
    ├── 중간 삽입 알림
    ├── 활성 세션 패널
    ├── 프로필 변경
    ├── 세션 초기화
    └── 관찰 종료

기록
    ├── 세션 목록
    └── 세션 상세

이벤트
    ├── 의심 이벤트 목록
    └── 의심 이벤트 상세

프로필
    ├── 프로필 목록
    ├── 무작위 생성
    ├── 프로필 상세
    └── 프로필 적용

설정
    ├── 관찰 설정
    ├── 알림 설정
    ├── 데이터 관리
    ├── 개인정보 안내
    └── WebView 진단
```

---

# 단계별 구현 계획

## 단계 A — 프로젝트 기반 구축

### 작업 내용

* Android 프로젝트 생성
* Kotlin과 Jetpack Compose 구성
* 의존성 버전 카탈로그 구성
* 화면 내비게이션 구성
* Room 데이터베이스 구성
* DataStore 설정
* 공통 오류 모델 정의
* 로그 수집 구조 정의
* 디버그와 릴리스 빌드 설정 분리

### 권장 패키지 구조

```text
app
core
    design
    database
    model
    webview
    observer
    profile
    export
feature
    onboarding
    observation
    sessions
    events
    profiles
    settings
```

### 핵심 산출물

* 앱 실행 가능 상태
* 빈 화면 간 이동
* 데이터베이스 생성
* 공통 테마 적용
* 기본 오류 로깅

### 완료 기준

* 앱이 정상 실행됨
* 모든 기본 경로로 이동 가능함
* 앱 재실행 후 설정값이 유지됨
* 데이터베이스 마이그레이션 테스트가 존재함

---

## 단계 B — 디자인 시스템 구현

### 작업 내용

첨부 디자인의 색상, 카드 구조, 상태 표시, 글꼴 계층을 Compose 테마로 옮긴다.

### 공통 컴포넌트

* `shorts monitorTopBar`
* `shorts monitorBottomBar`
* `PrimaryActionButton`
* `OutlinedActionButton`
* `StatusChip`
* `MetricCard`
* `SessionCard`
* `ProfileCard`
* `WarningCard`
* `EmptyState`
* `ErrorState`
* `LoadingState`
* `ConfirmationSheet`

### 카드 규칙

세션 카드와 이벤트 카드 왼쪽에 상태 색상 막대를 표시한다.

* 정상 세션: 녹색
* 의심 이벤트 포함: 주황색
* 오류 종료: 빨간색
* 관찰 중: 청록색

### 완료 기준

* 첨부 디자인과 동일한 계층 구조를 가짐
* 화면별 임의 색상 사용이 없음
* 상태 색상이 공통 토큰으로 관리됨
* 큰 글꼴에서도 텍스트가 잘리지 않음
* 다크 테마를 후속 추가할 수 있는 구조임

---

## 단계 C — 온보딩 화면 구현

### 화면 구성

첨부 온보딩 디자인을 기준으로 다음 내용을 표시한다.

* 앱 내부에서 유튜브 모바일 웹 실행
* 쇼츠 목록과 노출 순서 기록
* 기존 목록 사이의 신규 항목 감지
* 브라우저 테스트 프로필 변경
* 쿠키 및 사이트 데이터 초기화
* 기록의 로컬 저장

하단에는 다음 안내를 표시한다.

> 감지 결과는 관찰된 목록 변화 정황이며 플랫폼의 의도나 원인을 확정하지 않습니다.

### 사용자 동작

* 시작하기
* 개인정보 및 수집 항목 보기

### 저장 설정

온보딩 완료 여부를 DataStore에 저장한다.

### 완료 기준

* 최초 실행에서만 자동 표시됨
* 사용자가 개인정보 안내를 확인할 수 있음
* 시작하기 선택 후 관찰 홈으로 이동함
* 설정에서 온보딩을 다시 볼 수 있음

---

## 단계 D — 관찰 홈 화면 구현

### 화면 구성

첨부 관찰 홈 디자인을 그대로 반영한다.

#### 상태 요약 카드

* 현재 상태
* 현재 테스트 프로필
* 마지막 관찰 시각
* 전체 세션 수
* 최근 의심 이벤트 수

#### 주요 버튼

* 쇼츠 관찰 시작
* 최근 세션 이어보기

#### 빠른 작업

* 프로필 변경
* 세션 초기화
* 최근 이벤트
* 로그 내보내기

#### 최근 세션

* 관찰 시간
* 종료 상태
* 관찰 쇼츠 수
* 의심 이벤트 수
* 사용 프로필

### 상태 처리

* 기록 없음
* 최근 세션 없음
* 진행 중 세션 있음
* 복구 가능한 중단 세션 있음
* 데이터베이스 오류
* 내보내기 진행 중

### 완료 기준

* 데이터베이스 값이 화면에 반영됨
* 최근 세션을 선택하면 상세 화면으로 이동함
* 관찰 시작 시 새로운 세션이 생성됨
* 미완료 세션이 있으면 이어보기 또는 종료를 선택할 수 있음

---

## 단계 E — WebView 관찰 컨테이너 구현

### Compose 연동

Compose에는 전용 WebView 컴포저블이 없으므로 `AndroidView`로 표준 WebView를 감싸는 구조를 사용한다.

WebView 객체는 Compose 재구성마다 다시 만들지 않고 전용 컨트롤러가 생명주기를 관리한다.

### WebView 설정

* JavaScript 활성화
* DOM 저장소 활성화
* 쿠키 허용
* 미디어 재생 지원
* 파일 주소 접근 차단
* 혼합 콘텐츠 차단
* 안전하지 않은 외부 주소 차단
* 프로덕션 WebView 디버깅 비활성화
* 렌더러 종료 처리
* 뒤로 가기 처리

WebView는 완전한 브라우저와 동일하지 않으므로 일반 Chrome의 모든 특성을 재현한다고 전제하지 않는다.

### 허용 페이지

최상위 탐색은 유튜브 관련 HTTPS 주소로 제한한다.

외부 웹사이트 링크는 다음 중 하나로 처리한다.

* 외부 기본 브라우저에서 열기
* 사용자 확인 후 열기
* 차단

### 완료 기준

* WebView에서 유튜브 모바일 웹이 표시됨
* 쇼츠 화면으로 이동 가능함
* 스와이프와 영상 재생이 동작함
* 화면 회전 후 세션 상태가 손실되지 않음
* 렌더러 종료 후 복구할 수 있음

---

## 단계 F — JavaScript 관찰기 구현

### 관찰기 구성

페이지 내부에서 `MutationObserver`를 실행해 다음 변화를 감지한다.

* 쇼츠 항목 추가
* 쇼츠 항목 제거
* 항목 순서 변경
* 현재 활성 영상 변경
* 쇼츠 컨테이너 재생성
* 주소 변경
* 페이지 전체 재로드

### DOM 어댑터

유튜브 선택자는 화면 로직에 직접 작성하지 않는다.

```text
ShortsDomAdapter
    findFeedContainer
    findShortItems
    extractVideoId
    extractVideoUrl
    extractTitle
    extractChannel
    extractThumbnail
    detectActiveItem
```

선택자가 변경될 경우 어댑터만 교체할 수 있도록 한다.

### 스크립트 삽입

지원되는 환경에서는 문서 시작 시점 스크립트를 등록한다.

네이티브 통신 객체는 페이지를 열기 전에 등록하고 허용 출처를 제한한다. Android 공식 문서도 `addWebMessageListener`를 `loadUrl`보다 먼저 호출하고 허용 출처 규칙을 지정하는 방식을 안내한다.

### 메시지 유형

* 관찰기 준비 완료
* 현재 페이지 정보
* 활성 쇼츠 변경
* 목록 스냅샷
* DOM 재구성
* 관찰 오류
* 상태 확인 신호

### 완료 기준

* 쇼츠 목록을 JavaScript에서 추출할 수 있음
* 활성 영상 변경이 네이티브 앱으로 전달됨
* 같은 영상의 재렌더링이 중복 영상으로 저장되지 않음
* 관찰기 중단을 감지하고 재시작할 수 있음
* 잘못된 출처의 메시지가 무시됨

---

## 단계 G — 쇼츠 목록과 노출 기록 구현

### 기록 구분

shorts monitor는 다음 기록을 분리한다.

#### 현재 DOM 목록

현재 WebView 문서에서 관찰 가능한 쇼츠 순서다.

이는 유튜브 서버의 전체 추천 목록이 아니며 화면에서는 `현재 관찰 목록`으로 표시한다.

#### 누적 발견 목록

해당 세션에서 한 번이라도 발견한 쇼츠의 목록이다.

DOM에서 제거되어도 삭제하지 않는다.

#### 실제 노출 순서

사용자가 실제로 활성화해 본 쇼츠의 순서다.

같은 영상이 다시 노출되면 새로운 노출 이벤트를 생성한다.

### 쇼츠 식별 우선순위

* 쇼츠 주소의 영상 식별값
* 링크 주소
* DOM 데이터 속성
* 썸네일 주소
* 제목·채널·썸네일 조합 해시

### 저장 정보

* 영상 식별값
* 영상 주소
* 제목
* 채널명
* 썸네일 주소
* 최초 발견 시각
* 마지막 관찰 시각
* 활성화 시각
* 이전 영상
* 다음 영상
* 식별 신뢰 상태

### 완료 기준

* 같은 영상을 안정적으로 중복 제거함
* 노출 순서와 DOM 목록을 구분해 저장함
* 식별 실패 항목도 임시 식별값으로 기록함
* 앱 종료 후 기록을 다시 조회할 수 있음

---

## 단계 H — 중간 삽입 의심 탐지 구현

### 기본 비교

이전 목록:

```text
A → B
```

현재 목록:

```text
A → X → B
```

다음 조건이 충족되면 `X`를 후보로 등록한다.

* 이전 목록에서 `A`와 `B`가 인접함
* `X`가 이전 목록에 없음
* 현재 목록에서 `A`와 `B`가 유지됨
* `X`가 목록 끝이 아닌 중간에 있음
* 전체 페이지 재로드가 아님
* 프로필 변경 직후가 아님
* 세션 초기화 직후가 아님
* DOM 전체 재구성 직후가 아님

### 후보 안정화

후보를 발견한 즉시 사용자에게 알리지 않는다.

다음 스냅샷에서도 신규 항목과 앞뒤 관계가 유지될 때 의심 이벤트로 확정한다.

### 이벤트 상태

* 후보
* 확인됨
* 사용자 검토 완료
* 무효화됨
* 오탐

### 사용자 판정

* 의심 유지
* 정상 변화
* 오탐
* 판단 보류

### 완료 기준

* 목록 끝 추가를 의심 이벤트로 분류하지 않음
* 단순 DOM 재생성을 중간 삽입으로 오인하지 않음
* 동일 이벤트가 반복 생성되지 않음
* 판정 근거가 이벤트 상세에 저장됨
* 탐지 엔진을 고정 HTML 데이터로 단위 테스트할 수 있음

---

## 단계 I — 쇼츠 관찰 중 화면 구현

### 화면 구조

첨부 디자인과 같이 WebView 위에 네이티브 Compose UI를 겹쳐 표시한다.

WebView 내부 HTML로 제어 패널을 만들지 않는다.

#### 상단 상태 영역

* 관찰 중 상태
* 현재 프로필
* 관찰 쇼츠 수
* 의심 이벤트 수
* 관찰 종료

#### 중간 삽입 알림 카드

* 신규 영상 제목
* 채널명
* 감지 시각
* 자세히 보기
* 나중에 확인

#### 하단 활성 세션 패널

* 현재 영상 제목
* 채널명
* 영상 식별값
* 현재 순서
* 마지막 변화 시각
* 로그 열기
* 일시 중지
* 초기화

### 상호작용

* 패널 접기와 펼치기
* WebView 콘텐츠를 가리지 않도록 최소화
* 의심 알림을 닫아도 기록 유지
* 알림 선택 시 이벤트 상세 이동
* 시스템 뒤로 가기로 패널부터 닫기

### 완료 기준

* WebView 스와이프와 Compose 패널 터치가 충돌하지 않음
* 알림 카드가 영상 재생을 강제로 중단하지 않음
* 화면 크기에 따라 패널 높이가 조정됨
* 관찰 중 홈으로 이동해도 세션 상태가 유지됨

---

## 단계 J — 세션 기록 화면 구현

### 첨부 디자인 반영

세션 카드에는 다음을 표시한다.

* 세션 이름
* 시작 시각
* 관찰 시간
* 관찰 쇼츠 수
* 의심 이벤트 수
* 프로필 이름
* 종료 상태
* 내보내기 상태

### 필터

* 전체
* 의심 이벤트 있음
* 정상
* 오류
* 초기화 포함

### 검색

* 세션 이름
* 영상 제목
* 채널명
* 프로필 이름

### 세션 상세

* 세션 요약
* 실제 노출 순서
* 현재 목록 스냅샷
* 의심 이벤트
* 프로필 변경 이력
* 세션 초기화 이력
* 분석 오류
* 내보내기

### 완료 기준

* 대량 세션에서도 목록이 원활하게 스크롤됨
* 필터와 검색 조건이 함께 동작함
* 세션 삭제 전에 확인 절차가 표시됨
* 삭제된 세션의 하위 기록도 함께 정리됨

---

## 단계 K — 의심 이벤트 상세 화면 구현

### 첨부 디자인 반영

상단에 경고 카드와 대상 영상 정보를 표시한다.

* 영상 제목
* 채널명
* 영상 주소
* 발견 시각
* 발견 위치
* 사용 프로필

### 목록 비교

이전 목록:

```text
A
B
```

현재 목록:

```text
A
X
B
```

신규 항목은 경고색으로 강조하고 이전 앞뒤 항목과 하나의 그룹으로 표시한다.

### 판정 근거

* 이전 목록에 없음
* 중간 위치에 출현
* 앞뒤 기존 항목 유지
* 안정화 확인 완료
* 페이지 재로드 아님
* 프로필 변경 직후 아님
* 초기화 직후 아님

### 사용자 입력

* 의심 유지
* 정상 변화
* 오탐
* 판단 보류
* 사용자 메모

### 구현 보정

첨부 디자인의 `글로벌 무결성 지표`나 `영구 보존` 같은 표현은 최초 MVP에서 제거한다.

서버 동기화와 영구 감사 로그가 없으므로 실제 구현 범위를 넘어서는 표현을 사용하지 않는다.

### 완료 기준

* 변경 전후 스냅샷을 명확히 비교할 수 있음
* 사용자 판정 변경 이력이 저장됨
* 메모가 자동 저장됨
* 원본 자동 판정과 사용자 판정을 분리해 표시함

---

## 단계 L — 브라우저 테스트 프로필 구현

### 프로필 화면

첨부 디자인의 카드형 목록을 유지한다.

각 카드에는 다음 정보를 표시한다.

* 프로필 이름
* 활성 상태
* 기기 유형
* User-Agent 요약
* 화면 표현값
* 언어
* 시간대
* 마지막 사용 시각

### 프로필 생성

각 속성을 완전히 독립적으로 무작위 생성하지 않는다.

호환 가능한 템플릿을 먼저 선택하고 그 범위 안에서 값을 생성한다.

템플릿 예시:

* 소형 Android 모바일
* 일반 Android 모바일
* 대형 Android 모바일
* Android 태블릿

### 적용 항목

* User-Agent
* 지원되는 User-Agent 메타데이터
* 언어
* 언어 목록
* 시간대 표현
* 화면 크기 표현
* CPU 코어 노출값
* 메모리 노출값
* 터치 지원 표현

AndroidX WebKit은 WebView 호환 기능과 프로필 관리 API를 제공하지만, 실제 사용 가능 여부는 단말의 WebView 기능 검사 결과에 따라 분기해야 한다.

### 사용 중 프로필 변경

* 현재 목록 저장
* 현재 프로필 구간 종료
* WebView 로딩 중지
* 기존 WebView 제거
* 새 프로필 적용
* 새 WebView 생성
* 기존 주소 재로드
* 새로운 기준 목록 수집
* 탐지 재개

프로필 변경 직전과 직후 목록은 서로 비교하지 않는다.

### 완료 기준

* 무작위 프로필이 내부 일관성을 가짐
* 현재 프로필을 복제할 수 있음
* 프로필 적용 시 WebView가 새로 생성됨
* 지원되지 않는 변경값이 화면에 명시됨
* IP나 실제 하드웨어 변경으로 오해할 표현이 없음

---

## 단계 M — 쿠키와 사이트 데이터 초기화 구현

### 초기화 확인 화면

첨부 화면에는 별도 디자인이 없으므로 공통 하단 시트로 추가한다.

삭제 대상:

* 쿠키
* 로그인 상태
* Local Storage
* Session Storage
* Web Storage
* Web SQL 데이터
* WebView 캐시
* 탐색 기록
* 폼 데이터

유지 대상:

* shorts monitor 관찰 기록
* 사용자 판정
* 사용자 메모
* 저장된 프로필
* 내보낸 파일

Android의 `CookieManager`는 WebView 쿠키를 관리하고, `WebStorage.deleteAllData`는 JavaScript 저장소 데이터를 지우는 API를 제공한다.

### 초기화 흐름

* 현재 스냅샷 저장
* 관찰 일시 중지
* 초기화 이벤트 저장
* WebView 로딩 중지
* 페이지 저장소 정리
* 쿠키 제거
* Web Storage 제거
* 캐시 제거
* 기존 WebView 폐기
* 새 WebView 생성
* 초기화 검증
* 유튜브 시작 화면 로드
* 새 기준 목록 생성
* 관찰 재개

### 초기화 옵션

* 현재 프로필 유지
* 새 프로필 생성
* 초기화 후 쇼츠 열기
* 초기화 후 유튜브 홈 열기

### 완료 기준

* 기존 로그인 상태가 유지되지 않음
* 이전 사이트 저장 데이터가 복구되지 않음
* shorts monitor 관찰 로그는 유지됨
* 일부 초기화 실패 시 실패 항목을 표시함
* 초기화 중 중복 실행이 차단됨

---

## 단계 N — 내보내기 구현

### JSON 내보내기

다음 내용을 포함한다.

* 세션 정보
* 사용 프로필
* 실제 노출 순서
* 누적 발견 목록
* 목록 스냅샷
* 의심 이벤트
* 사용자 판정
* 사용자 메모
* 초기화와 오류 이력

### CSV 내보내기

파일을 목적별로 구분한다.

* 세션
* 쇼츠
* 노출 이벤트
* 의심 이벤트

### 제외 정보

* 쿠키 원문
* 인증 토큰
* 로그인 자격증명
* 전체 HTML
* 전체 캐시
* 영상 파일

### 완료 기준

* 시스템 파일 선택기로 저장 위치를 선택할 수 있음
* 파일 생성 중 앱 종료가 발생해도 기존 데이터가 손상되지 않음
* 내보내기 실패 원인을 표시함
* 동일 세션을 다시 내보낼 수 있음

---

## 단계 O — 설정과 진단 화면 구현

### 관찰 설정

* 목록 스냅샷 저장
* 의심 후보 안정화
* 메타데이터 저장
* 썸네일 주소 저장

### 알림 설정

* 앱 내부 배너
* 시스템 알림
* 진동
* 오류 알림
* 의심 이벤트 알림

### 데이터 설정

* 기록 보존 정책
* 세션 삭제
* 전체 데이터 삭제
* JSON 내보내기
* CSV 내보내기

### WebView 진단

* WebView 제공 패키지
* WebView 버전
* 지원 기능
* 현재 User-Agent
* 현재 프로필
* 관찰기 상태
* 마지막 상태 확인 시각
* 마지막 DOM 오류

### 완료 기준

* 설정 변경이 즉시 반영됨
* 위험한 삭제 작업은 확인 절차를 거침
* 진단 정보를 복사할 수 있음
* 민감한 쿠키나 인증값은 진단 정보에 포함되지 않음

---

## 단계 P — 테스트와 안정화

### 단위 테스트

* 영상 식별값 추출
* 임시 식별값 생성
* 목록 차이 계산
* 끝부분 추가 판정
* 중간 삽입 후보 판정
* 중복 이벤트 제거
* 후보 안정화
* 프로필 일관성 검사
* JSON 직렬화

### JavaScript 고정 데이터 테스트

유튜브 실페이지에만 의존하지 않고 고정 HTML 데이터를 사용한다.

테스트 상황:

* 최초 목록 생성
* 목록 끝 항목 추가
* 목록 중간 항목 추가
* 항목 제거
* 항목 순서 변경
* 같은 항목 재렌더링
* 컨테이너 전체 재생성
* 활성 영상 변경

### 통합 테스트

* WebView 메시지 수신
* Room 저장
* 프로필 변경
* 쿠키 초기화
* WebView 재생성
* 알림 표시
* 세션 복구
* 내보내기

### 실제 단말 테스트

* 서로 다른 화면 크기
* 서로 다른 WebView 버전
* 느린 네트워크
* 네트워크 재연결
* 앱 백그라운드 복귀
* 화면 회전
* 저장 공간 부족
* WebView 렌더러 종료
* 유튜브 DOM 변경

### 완료 기준

* 주요 사용자 흐름이 실제 단말에서 완료됨
* 치명적 오류 없이 세션을 종료할 수 있음
* WebView 오류가 관찰 기록 손실로 이어지지 않음
* 오탐 사례가 재현 가능한 테스트로 등록됨

---

# 데이터베이스 구성

## 관찰 세션

`observation_session`

* 세션 식별자
* 세션 이름
* 상태
* 시작 시각
* 종료 시각
* 시작 주소
* 종료 사유
* 앱 버전
* WebView 정보

## 쇼츠 항목

`observed_short`

* 내부 식별자
* 세션 식별자
* 영상 식별값
* 영상 주소
* 제목
* 채널명
* 썸네일 주소
* 식별 상태
* 최초 발견 시각
* 마지막 관찰 시각

## 노출 이벤트

`exposure_event`

* 세션 식별자
* 프로필 구간
* 영상 식별값
* 노출 시작
* 노출 종료
* 노출 순서

## 목록 스냅샷

`list_snapshot`

* 세션 식별자
* 프로필 구간
* 생성 시각
* 현재 주소
* 활성 영상
* 영상 배열
* 변경 사유
* DOM 리비전

## 의심 이벤트

`insertion_event`

* 세션 식별자
* 신규 영상
* 앞 영상
* 뒤 영상
* 변경 전 스냅샷
* 변경 후 스냅샷
* 감지 시각
* 자동 판정
* 사용자 판정
* 사용자 메모

## 브라우저 프로필

`browser_profile`

* 프로필 식별자
* 이름
* 템플릿 유형
* User-Agent
* 언어
* 시간대
* 화면 표현값
* 하드웨어 노출값
* 터치 표현값
* 생성 시각
* 마지막 사용 시각

---

# 구현 우선순위

## 필수 우선순위

* WebView에서 쇼츠 실행
* 활성 영상 식별
* 목록 스냅샷 생성
* 중간 삽입 의심 판정
* 관찰 중 화면
* 세션 기록
* 의심 이벤트 상세
* 프로필 변경
* 쿠키 초기화

## 후순위

* CSV 내보내기
* 복잡한 검색 필터
* 프로필 복제
* 고급 진단 화면
* 태블릿 분할 화면
* 다크 테마

---

# 주요 위험과 대응

## 유튜브 DOM 변경

대응:

* 선택자 중앙 관리
* 복수 선택자 후보
* 어댑터 버전 분리
* 진단 로그 저장
* 관찰 실패 시 앱 자체는 계속 사용 가능

## 가상 스크롤 오탐

대응:

* 현재 DOM 목록과 실제 노출 순서 분리
* 앞뒤 인접 관계로 판정
* 후보 안정화
* DOM 전체 재구성 제외
* 사용자 오탐 판정 제공

## WebView 기능 차이

대응:

* 실행 시 기능 지원 검사
* 지원되지 않는 기능의 대체 경로 구현
* 프로필 적용 결과 표시
* 최소 지원 WebView 정책 안내

## 세션 초기화 불완전

대응:

* 쿠키와 저장소를 별도로 제거
* 기존 WebView 폐기
* 새 WebView 생성
* 초기화 후 검증
* 부분 실패 결과 표시

## 디자인과 실제 WebView 충돌

대응:

* WebView 위 UI는 Compose 레이어로 구현
* 플로팅 패널 접기 제공
* 쇼츠 주요 조작 영역을 가리지 않음
* 화면 크기별 패널 위치 조정

---

# 최종 완료 조건

shorts monitor MVP는 다음 조건을 모두 만족할 때 완료한다.

* 첨부된 핵심 화면이 shorts monitor 명칭으로 구현됨
* 앱 내부 WebView에서 유튜브 쇼츠를 이용할 수 있음
* 관찰된 쇼츠 목록이 순서대로 저장됨
* 실제 노출 순서가 별도로 저장됨
* 목록 중간에 출현한 신규 항목이 후보로 탐지됨
* 후보 안정화 후 사용자에게 알림이 표시됨
* 사용자가 의심·정상 변화·오탐·판단 보류를 선택할 수 있음
* 브라우저 테스트 프로필을 생성하고 변경할 수 있음
* 프로필 변경 시 WebView가 다시 생성됨
* 쿠키와 사이트 데이터를 초기화할 수 있음
* 초기화 후에도 shorts monitor 관찰 기록은 유지됨
* 세션과 의심 이벤트를 조회할 수 있음
* JSON 및 CSV로 기록을 내보낼 수 있음
* WebView 오류나 DOM 분석 실패가 기존 기록 손실로 이어지지 않음
* 앱이 유튜브의 의도나 추천 조작을 자동으로 단정하지 않음
