# AGENTS.md

## 원칙

* 명시한 요청만 수행. 임의 추가 구현/스코프 확장 금지.
* 필요해 보여도 먼저 제안하고 승인 후 실행.
* 최소 변경, 최소 구현. 요청 안 한 리팩토링/오버엔지니어링 금지.
* 요청 파일 외 수정 금지. 기존 사용자 변경사항 덮어쓰기 금지.

## 출력·토큰 규칙

* 작업 중에는 아무 설명도 출력하지 말고, 완료 후 최종 결과만 출력해줘.
* 최종 결과는 20줄 이내로 제한.
* 코드 전문, diff 전문, 명령 출력 전문은 출력하지 마.
* 오류가 발생한 경우에도 원인과 조치만 2줄 이내로 요약.
* 모든 명령은 압축 플래그를 사용하여 실행한다.
* 이미 읽은 파일 중 변경이 없는 파일은 다시 읽지 않는다.

## Git

* 명령은 해당 저장소에서 실행.
* 편집 전 Git 상태 확인.
* 요청 없으면 파괴적 Git 명령 금지.
* 빌드 산출물 커밋 금지.
* 먼저 좁게 검증, API/공유 동작 변경 시 테스트 확대.

# 작업 종료 규칙

- 요청받은 구현 작업을 먼저 모두 완료한다.
- 모든 작업을 완료 후 아래 명령을 반드시 순서대로 실행한다.
- commit과 push 사이에 필요하지 않은 파일 탐색, 디렉터리 탐색, 테스트 재실행을 하지 않는다.
- push가 성공하면 즉시 작업 결과를 보고하고 종료한다.

```bash
git add -A
git commit -m "server"
git push
```